import { EsConfig } from './decorator/elasticsearch';
import { IMidwayApplication } from '@midwayjs/core';
import { CoolEventManager } from '@cool-midway/core';
import { ILogger } from '@midwayjs/logger';
import { Client } from '@elastic/elasticsearch';
import { CoolEsConfig } from '.';
/**
 * 搜索引擎
 */
export declare class CoolElasticSearch {
    app: IMidwayApplication;
    coreLogger: ILogger;
    esConfig: CoolEsConfig;
    client: Client;
    coolEventManager: CoolEventManager;
    init(): Promise<void>;
    scan(): Promise<void>;
    /**
     * 数据更新事件
     * @param method
     * @param data
     */
    esDataChange(method: any, data: any): Promise<void>;
    /**
     * 创建索引
     * @param cls
     * @param config
     */
    createIndex(cls: any, config: EsConfig): Promise<void>;
}
