import { Mode, CoolFileConfig } from './interface';
import { ILogger, IMidwayApplication } from '@midwayjs/core';
import * as OSS from 'ali-oss';
import * as COS from 'cos-nodejs-sdk-v5';
import * as QINIU from 'qiniu';
/**
 * 文件上传
 */
export declare class CoolFile {
    config: CoolFileConfig;
    coreLogger: ILogger;
    client: OSS & COS & QINIU.auth.digest.Mac;
    app: IMidwayApplication;
    init(): Promise<void>;
    /**
     * 上传模式
     * @returns 上传模式
     */
    getMode(): Promise<Mode>;
    /**
     * 获得原始操作对象
     * @returns
     */
    getMetaFileObj(): OSS & COS & QINIU.auth.digest.Mac;
    /**
     * 下载并上传
     * @param url
     */
    downAndUpload(url: string): Promise<any>;
    /**
     * 上传文件
     * @param ctx
     */
    upload(ctx: any): Promise<unknown>;
    /**
     * 七牛上传
     * @param ctx
     * @returns
     */
    private qiniu;
    /**
     * OSS 文件上传
     * @param ctx
     */
    private oss;
    /**
     * COS 文件上传
     * @param ctx
     */
    private cos;
    /**
     * 本地上传
     * @param ctx
     * @returns
     */
    private local;
}
