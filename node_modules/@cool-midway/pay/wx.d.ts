import * as Tenpay from 'tenpay';
import { CoolWxPayConfig } from './interface';
/**
 * 微信支付
 */
export declare class CoolWxPay {
    pay: Tenpay;
    coolWxPay: CoolWxPayConfig;
    init(): Promise<void>;
    /**
     * 获得微信支付SDK实例
     * @returns
     */
    getInstance(): Tenpay;
    /**
     * 签名
     * @param json
     * @returns
     */
    signVerify(json: any): boolean;
    /**
     * 创建订单
     * @param length 订单长度
     * @returns
     */
    createOrderNum(length?: number): any;
    /**
     * 动态配置支付参数
     * @param config 微信配置
     * @returns
     */
    initPay(config: CoolWxPayConfig): any;
}
