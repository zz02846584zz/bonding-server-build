import { ILogger, IMidwayApplication } from '@midwayjs/core';
import { CoolModuleConfig } from './config';
/**
 * 模块sql
 */
export declare class CoolModuleImport {
    ormConfig: any;
    coolConfig: any;
    coreLogger: ILogger;
    coolModuleConfig: CoolModuleConfig;
    app: IMidwayApplication;
    init(): Promise<void>;
    /**
     * 导入数据库
     * @param module
     * @param lockPath 锁定导入
     */
    initDataBase(module: string, lockPath: string): Promise<void>;
    /**
     * 检查数据库版本
     */
    checkDbVersion(): Promise<void>;
}
