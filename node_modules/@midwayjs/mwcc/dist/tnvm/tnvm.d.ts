export declare class TnvmAgent {
    private tnvmDir;
    private shell;
    private enabled;
    constructor(tnvmDir?: string);
    listVersions(type: string): Promise<string[]>;
    isVersionInstalled(version: string): Promise<boolean>;
    getExecPathOfVersion(version: string): Promise<string | undefined>;
    install(version: string): Promise<string>;
    uninstall(version: string): Promise<string>;
    private exec;
    private findBash;
    private assertPlatform;
}
//# sourceMappingURL=tnvm.d.ts.map