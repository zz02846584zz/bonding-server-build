"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.closestAncestor = void 0;
const typescript_1 = __importDefault(require("typescript"));
function closestAncestor(node, kinds) {
    if (node == null) {
        return;
    }
    if (!Array.isArray(kinds)) {
        kinds = [kinds];
    }
    kinds = kinds.map(it => {
        if (typeof it === 'string') {
            return typescript_1.default.SyntaxKind[it];
        }
        return it;
    });
    let parent = node.parent;
    while (parent != null) {
        node = parent;
        if (kinds.indexOf(node.kind) >= 0) {
            return node;
        }
        parent = node.parent;
    }
    return undefined;
}
exports.closestAncestor = closestAncestor;
//# sourceMappingURL=ancestor.js.map